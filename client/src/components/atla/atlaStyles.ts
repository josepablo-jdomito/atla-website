import type { CSSProperties } from "react";

/** Pill call-to-action used by the home hero and the contact page. Color and border are set per surface. */
export const ATLA_PILL: CSSProperties = {
  fontFamily: "'Libre Franklin', Helvetica, sans-serif",
  fontSize: 14,
  fontWeight: 600,
  letterSpacing: 0.3,
  lineHeight: 1,
  textDecoration: "none",
  borderRadius: 999,
  minHeight: 48,
  padding: "0 22px",
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  whiteSpace: "nowrap",
};
