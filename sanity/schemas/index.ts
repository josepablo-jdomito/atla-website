import brand from './brand'
import category from './category'
import post from './post'
import homepageConfig from './homepageConfig'

export const schemaTypes = [brand, category, post, homepageConfig]
